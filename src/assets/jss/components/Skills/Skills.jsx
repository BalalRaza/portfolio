const styles = (theme) => {
  return {
    root: theme.section.root,
    heading: theme.section.heading,
    grid: {
      display: 'flex',
      flexWrap: 'wrap',
    },
  };
};

export default styles;
